为这组已有笔记添加实体链接。笔记原样保留，不拆分或重复输出。只处理本组笔记涉及的实体。

你是唯一身份判定者。一个实体只能代表一个对象；canonical是单个对象的专名，aliases只能是原文明示同一对象的其他专名。两个组织即使同属一种类型也必须是两个实体。组织与所属阵营、上下级、物品类别和子类都不是同一对象。不能仅按名称相似认定身份。姓名保持原文繁简字。

existing_entities来自本章先前批次的身份判定。证据支持同一对象时复用其显式id，不要新建重复实体；不能合并两个existing id。不确定就记入对应笔记的unresolved，不猜身份。新实体id必须以给定new_id_prefix开头。kind只能取ontology.kinds；找不到适当类型的概念保留在笔记中，不要强塞进item。

每个本组使用的实体输出一行，canonical及aliases须在所引用的passages中有原文见证，最多3段。只选最小身份见证，不列每次提及。不要输出英文名、原文全文、解释或事实清单。

只返回JSON：{"entities":[["b1e1","character","原文专名",["同一对象的别名"],["p001"]]],"links":[["n1",["b1e1"],[]]]}。
entities每行依次为显式ID、kind、canonical、aliases数组、passage ID数组。无别名时aliases为空数组。links每行依次为原笔记ID、实际涉及的实体ID数组、未定身份的表述数组。每条笔记必须恰好一行；每个链接必须使用本响应实体行的显式ID，不是数组位置。
