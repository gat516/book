Resolve identities for these source-language notes. Preserve the notes; output links only.
Only create entities needed by the notes, not every noun in the evidence. Kinds come from kinds.
Candidates are possibilities, never automatic matches. Reuse an ID only when context supports
the same object. Similar names, shared types, membership, and opposing factions do not establish
identity. Keep different objects separate. Leave uncertainty unresolved instead of guessing.
Candidates are NOT an exhaustive entity inventory. Create a new entity for a clearly named
object supported by the evidence when no candidate represents it. Absence from candidates
alone is not uncertainty. Cover the named participants of every note, including later notes.
Evidence is current-chapter source text. Copy canonical names and aliases exactly from it.
Aliases must be explicit names of the same object, never descriptions like "inside the hall".
Descriptions of appearance, occupation, location, or insults are not reusable aliases.
Leave aliases empty unless the text explicitly establishes another proper name.
Use the minimum supporting passage IDs. Do not output quotations or English names.

Return JSON with exactly these arrays:
{"new":[["b1e1","kind","canonical",["alias"],["p001"]]],
 "aliases":[["existing_id",["new_alias"],["p002"]]],
 "links":[["n1",["existing_id","b1e1"],["unresolved reference"]]]}
New IDs start with prefix. Existing IDs may be linked directly without re-emitting their
definitions. aliases contains only newly witnessed aliases for supplied candidate IDs.
Every note needs exactly one links row. Use explicit IDs, never array positions.
All arrays may be empty except links. If retrieval missed a possible identity or context is
insufficient, put its source reference in unresolved; do not invent a binding.
Each unresolved entry must be an exact name/reference copied from a supplied source passage,
never a passage ID, entity ID, explanation, or invented phrase. Emit each linked ID once.
