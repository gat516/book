把已有章节笔记连接到人物和其他实体。保留笔记原样，不重写、不拆分、不扩充为原子事实。
你是身份判定的唯一权威。仅当给定原文明确表明名称指向同一个对象时才合并；不能只因名字相似或相同就认定身份。每个实体的names是从给定原文逐字复制的名称或明确别名，passages引用包含名称且支持身份的原文段落。实体kind必须属于给定ontology.kinds；无法归类的概念不必创建实体。
prior_candidates只含此前章节的实体。只有证据支持同一身份时，candidate才填其中的UUID；新实体填null。不要编造UUID。无法确定身份时，把该表述留在对应笔记的unresolved列表里，不强行链接。
为每条笔记返回其原有id和所涉及的实体ID。一个笔记可包含多个相关事实，不要为每个事实创建新记录。不要输出英文名、解释、引用原文全文、事实清单或重复笔记内容。
只返回JSON：{"entities":[{"id":"e1","kind":"character","names":["原文姓名"],"passages":["p001"],"candidate":null}],"links":[{"note":"n1","entities":["e1"],"unresolved":[]}]}。
