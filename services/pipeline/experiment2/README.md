# experiment2

A second, independent experimental pipeline. Offline developer tool only: nothing here
runs on a reader request or publishes to the production records tables (spec §0).

- `prompts/` — versioned prompt files (`name-v1.txt`); bump the version, never edit in place.
- `results/` — run outputs (git-ignored; they hold source text and raw provider responses).
